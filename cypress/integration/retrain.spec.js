// / <reference types="Cypress" />
describe('Retrain flow', () => {
  // beforeEach(() => {
  //   cy.clearLocalStorage();
  //   cy.startServer();
  // });
  // it('Successfully loads not-logged in state ', () => {
  //   cy.visit('/project/new');
  //   cy.get('body');
  //   cy.get('header');
  //   cy.get('[data-cy=primary-panel]').should('exist');
  //   cy.get('[data-cy=secondary-panel]').should('exist');
  //   cy.get('[data-cy=leaflet-map]').should('exist');
  //   cy.get('[data-cy=select-model-label]').should(
  //     'have.text',
  //     'Login to select model'
  //   );
  // });
  // it('Open project and run inference', () => {
  //   cy.fakeLogin();
  //   cy.visit('/project/1');
  //   cy.get('[data-cy=select-model-label]').should(
  //     'have.text',
  //     'NAIP Supervised'
  //   );
  // cy.get('[data-cy=run-model-button]').click();
  // wait for inference completion
  // click in a class
  // add samples
  // retrain
  // });
});
